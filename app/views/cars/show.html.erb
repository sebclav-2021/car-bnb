<div class="container">
  <h1>Choose your period of rental</h1>

  <ul>
    <p><%= image_tag @car.car_picture, width: 400 %></p>
    <h1><%= @car.brand %> <%= @car.model %></h1>
    <p><%= @car.price %>€ per day</p>
  <ul>

  
    <%= simple_form_for([@car,@booking]) do |f| %>
      <%= f.input :start_date %>
      <%= f.input :end_date %>
      <%= f.submit "Book", class: "button-blue" %>
    <% end %>
</div>